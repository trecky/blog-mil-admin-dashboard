{
  "name": "blogmil_dashboard",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "yarn --cwd packages/app build",
    "dev:server": "yarn --cwd packages/server server",
    "dev:app": "yarn --cwd packages/app start"
  }
}
