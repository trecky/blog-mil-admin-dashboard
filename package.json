{
  "name": "blogmil_dashboard",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "yarn --cwd packages/app build"
  }
}
